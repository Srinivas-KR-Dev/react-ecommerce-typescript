# This project currently uses Vite's dev server proxy for API and image requests.
# Frontend code calls relative `/api` and `/images` paths.
# In local development, `vite.config.ts` proxies those requests to `http://localhost:7000`.
#
# No frontend environment variables are required for the current setup.
